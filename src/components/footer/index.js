import classes from './styles.module.css';

const Footer = () => {
    return <div className={classes.footerContainer}>
        <p className={classes.easy}>EASY.</p>
        <p className={classes.quick}>QUICK.</p>
        <p className={classes.secure}>SECURE.</p>

    </div>;
};

export default Footer;

