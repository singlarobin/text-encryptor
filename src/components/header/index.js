import classes from './styles.module.css';

const Header = () => {
    return <div className={classes.headerContainer}>
        <div className={classes.headerContent}>Sw<span>oo</span>sh</div>
    </div>;

};

export default Header;