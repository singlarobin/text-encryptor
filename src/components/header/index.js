import classes from './styles.module.css';

const Header = ()=>{
    return(
        <div className={classes.container}>
            <div className={classes.header}>Sw<span>oo</span>sh</div>
        </div>
    );

};

export default Header;